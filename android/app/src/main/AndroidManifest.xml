<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Volta do login: o site, depois de autenticar no navegador,
                 manda o usuário para com.smartbalanco.app://login?codigo=...
                 e é este filtro que faz o Android devolver ao app.
                 O login do Google não roda em WebView, então ele acontece
                 no navegador de verdade e só o resultado volta para cá. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.smartbalanco.app" />
            </intent-filter>
        </activity>

        <!-- Widget da tela inicial. exported="true" é obrigatório: quem
             dispara o onUpdate é o sistema, de fora do app. -->
        <receiver
            android:name=".SmartbalancoWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_smartbalanco_info" />
        </receiver>

        <!-- Widget "+": atalho direto para o menu de lançamento -->
        <receiver
            android:name=".NovoLancamentoWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_novo_info" />
        </receiver>

        <!-- Widget 1x5: cinco atalhos do app -->
        <receiver
            android:name=".AcoesWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_acoes_info" />
        </receiver>

        <!-- Serviço que preenche a lista do widget de despesas.
             A permissão BIND_REMOTEVIEWS é exigida pelo sistema. -->
        <service
            android:name=".DespesasWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notificações locais dos vencimentos.
         POST_NOTIFICATIONS: obrigatória do Android 13 em diante; sem ela o
         aviso é criado e nunca aparece.
         SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM: o agendamento usa
         allowWhileIdle para o aviso não escorregar quando o aparelho está
         dormindo.
         RECEIVE_BOOT_COMPLETED: sem isso os agendamentos morrem quando o
         celular é reiniciado. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
